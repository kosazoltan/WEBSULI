services:
  - type: web
    name: websuli-api-eu
    runtime: node
    region: frankfurt
    rootDir: source
    buildCommand: npm install --include=dev --legacy-peer-deps && npm run build
    startCommand: node dist/index.js
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production