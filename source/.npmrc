# ESLint 10 + eslint-plugin-react-hooks@5 (rules-of-hooks). The plugin's
# peer range stops at ESLint 9; the rule itself loads on 10. Without this
# flag, `npm ci` in GitHub Actions fails ERESOLVE and the #310 gate never
# installs.
legacy-peer-deps=true
